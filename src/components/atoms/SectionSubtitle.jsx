import React from "react";

const SectionSubtitle = ({ children }) => (
  <h2
    style={{
      fontFamily: "Poppins",
      fontSize: "1.6rem",
      color: "white",
      marginTop: "2rem",
      marginBottom: "1rem",
    }}
  >
    {children}
  </h2>
);

export default SectionSubtitle;
